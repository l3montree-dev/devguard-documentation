---
disableToc: true
seo:
  keyword_primary: "package security analysis"
  keywords_secondary:
    - "open source package inspector"
    - "package vulnerability details"
    - "purl package security"
  og:
    type: "article"
---

import PurlPageComponent from '@/components/package-inspector/purlPage'
import { useRouter } from 'next/router'

export const PurlPage = () => {
    const router = useRouter()
    const { purl } = router.query
    const purlString = Array.isArray(purl) ? purl[0] : purl
    return (
        <>
            <style jsx global>{`
                #sidebar { display: none !important; }
                #toc { display: none !important; }
            `}</style>
            <PurlPageComponent purl={purlString} />
        </>
    )
}

<PurlPage />
