---
title: "Container Scanning GitLab Component"
description: "Reference for the DevGuard container-scanning GitLab component — scan Docker images for known vulnerabilities and generate SBOMs directly in your CI pipeline."
seo:
  robots: "index,follow"
  og:
    title: "Container Scanning GitLab Component"
    description: "Reference for the DevGuard container-scanning GitLab component — scan Docker images for known vulnerabilities and generate SBOMs directly in your CI pipeline."
    image: "/og-image.png"
    type: "article"
    schema:
      type: "TechArticle"
  keyword_primary: "container scanning GitLab component"
lang: "en-US"
ignoreChecks: []
---

# Container Scanning Gitlab Component

The container-scanning component scans your container images for vulnerabilities. This ensures that your Docker images do not contain known vulnerabilities before they are deployed.

The `container-scanning` component accepts the following inputs:

| Name        | Description                     | Required     | Default Value        |
|-------------|---------------------------------|-------------|----------------------|
| `api_url`   | The DevGuard API URL             | No           |       `https://api.devguard.org`     |
| `asset_name`| Name of the asset to scan                    |      Yes  |      |
| `token`     | API token for authenticating with DevGuard   |      Yes   |      |
| `scan_stage`| The stage where the scan is run       | No       |     `test`     |
| `runner_tags` | The runner tags used to select appropriate CI runners.   | No  | `""` |
| `fetch-image-from-registry` |If set to true, the image will be pulled from the registry instead of using the artifact.   | No          | `false`                                              | 



## Usage Example:
Here's an example of how to call this component:
```yaml copy
include:
- remote: "https://gitlab.com/l3montree/devguard/-/raw/main/templates/container-scanning.yml@main
  inputs:
    asset_name: "myOrganization/projects/myProject/assets/myAsset"
    token: "$DEVGUARD_TOKEN"
```
